# Current requirement

When an order is shipped, send the customer an email and record whether delivery succeeded or failed. Existing email behavior must remain unchanged and provider errors must still propagate to the caller.
