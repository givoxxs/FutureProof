{
  "name": "@futureproof/notification-demo-base",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\"",
    "build": "tsc -p tsconfig.json --noEmit"
  }
}
